{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "jq -r '\"[\" + (now|localtime|strftime(\"%Y-%m-%d %H:%M:%S\")) + \"] \" + .tool_name + \" \" + (.tool_input.file_path // .tool_response.filePath // \"unknown\")' >> \"C:/SDD_Demo/changelog.txt\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
